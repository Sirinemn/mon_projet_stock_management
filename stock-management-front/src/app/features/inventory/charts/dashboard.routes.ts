import { Routes } from '@angular/router';
import { DashboardComponent } from './components/dashboard/dashboard.component';

export const dashboard_routes: Routes = [
    { title: 'Dashboard', path: 'view', component: DashboardComponent }
]